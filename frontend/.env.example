# Optional. Leave unset for same-origin `/api/*` (Vercel + local `next dev` with rewrites).
# Set only if the browser must call a different host, e.g.:
# NEXT_PUBLIC_API_BASE_URL=https://your-api.example.com
